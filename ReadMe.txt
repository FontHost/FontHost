Install fonts by running the following command below

CD into the primary font location for macOS
curl to download the font

This Github serves as a host for the fonts


cd ~/Library/Fonts && { 
    curl -O 'FontWebLink'
    cd -; }
    
